<h2>Edit registration</h2>

<%= render "form.html", changeset: @changeset,
                        action: register_path(@conn, :update, @owner) %>

<%= link "Back", to: owner_path(@conn, :index, @owner) %>
